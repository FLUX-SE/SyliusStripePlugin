{#
    @deprecated since 1.0, to be removed in 2.0.
    The "secret_key" hook is deprecated. Both API keys are now rendered by the "publishable_key" hook.
    Remove any custom templates registered for this hook — they will no longer be rendered by default.
#}
