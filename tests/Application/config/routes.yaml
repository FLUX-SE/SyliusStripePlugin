flux_se_sylius_stripe_shop:
    resource: "@FluxSESyliusStripePlugin/config/shop_routing.yaml"
    prefix: /{_locale}
    requirements:
        _locale: ^[a-z]{2}(?:_[A-Z]{2})?$

flux_se_sylius_stripe_admin:
    resource: "@FluxSESyliusStripePlugin/config/admin_routing.yaml"
    prefix: /admin
